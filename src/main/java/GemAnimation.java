public class GemAnimation {
    public double time;
    public double total;

    GemAnimation() {
        this(0, 0);
    }

    GemAnimation(double time, double total) {
        this.time = time;
        this.total = total;
    }
}
